# StevenA's mksh config

These are my startup scripts for Mir KornShell. 

# Installation

Run bin/install. This creates symbolic links to the repo files. It assumes the repo is cloned to ~/.config/mksh-dot-files.
